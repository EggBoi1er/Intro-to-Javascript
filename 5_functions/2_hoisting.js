function isHappy(grade) {
    // if(grade > 90) {
    //     return true;
    // }
    // return false;
    //if else statement only using return 
    // grade > 90 is the condition and if its true itll return i am very happy
    // else itll return the other string
    return grade > 90 ? "I am very happy" : "not really! I am upset"
}
console.log(isHappy(91));
console.log(isHappy(50));
